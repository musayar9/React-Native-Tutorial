import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Box = () => {
  return (
    <View
      style={{
        width: "90%",
        height: 300,
        backgroundColor: "#50b8e8",
        borderRadius: 50,
        margin:5
      }}
    >
  
    </View>
  );
};

export default Box;

const styles = StyleSheet.create({});
